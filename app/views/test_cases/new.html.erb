<%
  breadcrumb = {'Home': root_url, 'Environments' => environments_url, 'Automate Test Cases' => environment_test_suite_manual_test_cases_path(@environment, @test_suite) }
  btns = []
%>
<%= render partial: 'common/breadcrumb', locals: {heading: 'Create Automate Test Case', breadcrumb: breadcrumb, action_btns: btns} %>
<section class="section min-vh-100">
  <div class="row">
    <div class="col-lg-12">
      <div class="card">
        <div class="card-body">
          <p class="card-title">Environment: <%= @environment.name %></p>
          <div class="mt-3">
            <%= render partial: 'test_cases/form' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>