
$('.test-assignment-user-content').html('<%= j(render partial: 'test_suites/assign_users_frm', locals: {}) %>');
<% if @error %>
$('#error').html('<%=j(render(partial: 'layouts/flash_message')) %>');
<% end %>
var paper_submission_modal = new bootstrap.Modal(document.getElementById('test-assignment-user'))
paper_submission_modal.show();

$('#test-assignment-user #user_email').select2({
    dropdownParent: $('#test-assignment-user'),
    theme: "bootstrap",
    placeholder: "Search and select team member by name",
    width: '100%',
    multiple: true,
    ajax: {
        url: '<%= fetch_team_members_path(format: :json, format_id: true) %>',
        dataType: 'json'
    },
})